# 研迹 v1.0 Codex 开发规格

> 用途：将本文件完整交给 Codex，在当前仓库内实施下一阶段开发。
>
> 推荐执行方式：严格按“阶段 A → 验证 → 阶段 B → 完整验证”的顺序推进。若上下文或时间不足，优先完整交付阶段 A，不要留下半完成的数据模型或不可用入口。

---

## 可直接交给 Codex 的开发指令

你正在维护 Windows/Electron 本地优先科研工作台“研迹”。当前仓库为：

`C:\Users\86262\Documents\ChatGPT\论文追踪`

当前版本为 v1.0.2，数据 schema 为 7。已有能力包括周日程看板、3:4 当日日程桌面组件、提醒、打卡、专注、应用使用统计、笔记与元数据、即写即走、快捷新建悬浮便笺、Elsevier 投稿与出版追踪、稿件截止任务、R0/R1/R2 修回轮次、投稿历程、本地加密、数据位置迁移、应用更新和 NSIS 安装包。

本轮目标不是继续增加彼此孤立的页面，而是完成“课题—笔记—日程—专注—投稿”的科研工作闭环，并先补齐影响数据安全和发布质量的可靠性问题。

### 一、开始前必须完成的检查

1. 阅读 `README.md`、`package.json`、`src/main.js`、`src/store.js`、`src/paper-core.js`、`src/workbench-core.js`、`src/preload.js`、`src/renderer/index.html`、`src/renderer/app.js`、`src/renderer/workbench.js`、`src/renderer/styles.css` 以及全部测试。
2. 运行 `git status --short`，保留用户现有的未跟踪文件和无关修改，不得清理、覆盖或提交它们。
3. 运行现有自动测试，记录修改前基线。当前预期为 65 项测试通过；若基线不同，先说明，不要伪造结果。
4. 检查现有 Electron 冒烟脚本的启动和截图时序。不得把“DOM 节点存在”视为已经完成可视化验证。

### 二、不可破坏的产品与安全边界

1. 保持 Windows/Electron、本地优先、无账号、无云同步的架构。
2. 不加入手机端、在线协作、完整 AI 写作、期刊 RSS、PDF 管理或编辑部后台。
3. 不修改现有 Elsevier 请求语义、追踪链接解析方式或生产页查询行为，除非是有测试覆盖的兼容性修复。
4. 不在 renderer、通知、日志、错误文本、导出或截图中暴露完整追踪链接、UUID、Cookie、token、作者查询凭证或加密密钥。
5. 继续使用 Electron `safeStorage`；不得把 DPAPI 密文描述为可跨 Windows 用户或跨电脑直接恢复的便携凭证。
6. 数据迁移失败时绝不能覆盖原数据文件。恢复备份前也必须先为当前数据创建可恢复副本。
7. 不恢复必应壁纸、大型顶部时间卡、固定早中晚打卡、系统原生 `alert/confirm/prompt`、Emoji 图标或便携版安装包。
8. 不得恢复文献推荐页面、OpenAlex 推荐请求、推荐 IPC 或其占位入口；后续科研闭环不依赖推荐流。
9. 保持当前淡蓝色、低饱和视觉方向，并继续支持 `prefers-reduced-motion`。
10. 新增外部链接只能通过主进程校验后的 `http:` 或 `https:` 地址打开。

---

## 阶段 A：先完成 v1.0.1 可靠性修补

阶段 A 必须能够独立发布。完成并验证后，再开始阶段 B。

### A1. 保持文献推荐完整移除

1. 导航、页面、样式、renderer 逻辑、preload API、IPC、主进程网络请求和冒烟测试中均不得保留文献推荐功能。
2. 后续不得以占位页、隐藏入口或“即将上线”提示重新引入推荐流。
3. 如未来确需文献能力，必须作为新的明确需求重新定义数据来源、隐私边界与验收标准，不能沿用已删除实现。

### A2. 笔记草稿与误关闭保护

1. 为新建和编辑笔记增加本地草稿：至少每 2 秒防抖保存一次，应用退出或崩溃后可以恢复。
2. 草稿保存在主进程管理的数据结构中，不在 URL、日志或通知中出现笔记正文。
3. 保存笔记后清除对应草稿。
4. 点击遮罩、关闭按钮、Esc 或切换页面时，如果存在尚未保存的修改，显示研迹自定义确认框，提供：
   - 保存并关闭；
   - 保留草稿并关闭；
   - 放弃修改。
5. 不得因为元数据字段增删导致正文草稿丢失。
6. 增加重启恢复、保存后清理、放弃草稿和中文输入法回归测试。

### A3. 自动备份与安全恢复

1. 增加版本化本地快照，不把“迁移存储位置后保留的旧文件”冒充日常备份。
2. 建议保留策略：最近 7 个每日快照和最近 4 个每周快照；不得在每次 5 秒应用采样时创建快照。
3. 快照必须先写临时文件、验证可解析和可迁移，再原子重命名。
4. 设置页增加“备份与恢复”：显示备份时间、数据版本、稿件/日程/笔记数量和文件大小。
5. 恢复前先备份当前数据；恢复文件解析、迁移或结构校验失败时，不得修改当前数据。
6. 增加“导出整个工作台的安全归档”：默认排除 `trackingSecret`、完整追踪 URL、UUID、token、Cookie 和密钥。导入安全归档后，需要用户重新录入无法便携迁移的投稿凭证。
7. 增加备份轮换、损坏备份、恢复失败不覆盖、恢复成功和脱敏导出的测试。

### A4. 保持周日程看板的可见性和可用性

1. 日程页采用按日期分列的一周看板，不再以大面积空白的 24 小时横轴作为主视图。
2. 每个事件卡完整显示时间范围、标题、优先级、Deadline、完成状态和跨日状态；点击日期后显示当天完整清单。
3. 跨午夜事件必须同时出现在涉及的日期列，并按当天边界显示时间。
4. 多个重叠事件纵向排列为独立卡片，不能互相遮挡。
5. 支持把“当日日程”作为固定 360 × 480（3:4）的 Windows 桌面组件打开。组件必须挂载到承载桌面图标的 Shell 窗口层，不得设置始终置顶，不进入任务栏或 Alt+Tab；显示当日完整日程、完成进度，并随本地日程变更同步刷新。若原生桌面层挂载失败，应明确提示并安全退化，不能静默伪装成已挂载。
6. 冒烟测试除检查节点数量外，还要检查事件卡：
   - `display`、`visibility`、`opacity` 有效；
   - 宽高大于 0；
   - 至少一个事件的矩形与看板可视区域相交；
   - 截图前页面确实处于“日程”。

### A5. 颜色对比度和键盘可用性

1. 检查所有 10–12 px 辅助文字、按钮文字、日期、作者、引用数和占位提示。
2. 普通文字与背景的对比度目标至少 4.5:1；大字号文字至少 3:1。
3. 保持淡蓝色视觉方向，优先通过文字明度与边框层级改善可读性，避免重新引入暖棕色主色。
4. 所有按钮、输入框、选项卡、对话框和导航必须有清晰的 `:focus-visible` 状态。
5. 对话框打开后聚焦第一个合理控件；关闭后把焦点还给触发按钮；Esc 行为一致。
6. 为主要页面增加基础键盘导航和可访问名称测试。

### A6. 让冒烟和视觉测试真正可依赖

1. Renderer 完成全部初始化后设置明确的 ready 标志或发送 ready 事件；冒烟脚本必须等待 ready，不能只等待固定毫秒数。
2. 截图每个页面前同时验证：
   - 对应导航项为 active；
   - 对应 `[data-page]` 可见；
   - 页面标题符合预期；
   - 截图文件非空且尺寸正确。
3. 修复投稿管理截图实际停留在主页仍能通过的问题。
4. 在 `package.json` 中加入清晰命令，例如：
   - `test:unit`；
   - `test:smoke`；
   - `test:visual`；
   - `test:all`。
5. 自动生成的测试数据和截图必须写入临时目录或专用输出目录，不得污染用户项目文件。

完成阶段 A 后：运行全部单元测试、所有页面冒烟测试，并实际检查主页、日程、笔记、投稿管理和设置页截图。阶段 A 未全部通过时，不要开始阶段 B。

---

## 阶段 B：实施 v1.0 科研工作闭环

### B1. 新增可选“研究项目/课题”实体

新增 `projects` 数据模型，至少包含：

- `id`；
- `name`；
- `description`；
- `color`（从现有低饱和色板选择）；
- `archivedAt`；
- `createdAt`；
- `updatedAt`。

为下列记录增加可选 `projectId`：

- schedules；
- notes；
- papers；
- focusSessions；

要求：

1. 未设置项目的旧数据正常工作，不强迫用户整理历史记录。
2. 删除项目时不得连带删除日程、笔记或稿件；只解除关联，优先提供“归档项目”。
3. 项目选择器保持轻量，支持搜索和“无项目”。
4. 项目详情页显示：下一步行动、最近笔记、稿件状态和本周专注时间，但不要复制原始数据。

### B2. 建立统一行动队列

主页“今日重点”不能再只读取普通日程。新增统一的 `ActionItem` 查询层，将以下内容投影为统一行动项：

- 当天未完成日程；
- 已逾期和 48 小时内到期的稿件任务；
- 需要作者处理的投稿/出版状态；
- 用户手动置顶的阅读或笔记行动。

每个行动项至少包含：

- `sourceType`；
- `sourceId`；
- `projectId`；
- `title`；
- `dueAt`；
- `priority`；
- `status`；
- `navigationTarget`。

要求：

1. 不复制稿件任务为第二条普通日程，避免两份完成状态互相冲突。
2. 在主页完成行动时，必须更新原始记录。
3. 点击行动项必须打开正确页面和正确编辑对象。
4. 排序规则明确且有测试：逾期 > 今日截止 > 需作者处理 > 普通今日安排 > 未来事项。

### B3. 专注计时关联项目和任务

1. 开始专注前可选择项目，并可选选择一个统一行动项。
2. `focusSessions` 保存 `projectId` 和稳定的来源引用，例如 `actionSourceType/actionSourceId`，不得保存易变的显示文本作为唯一关联。
3. 专注结束后允许填写不超过 500 字的“本次产出”。
4. 项目详情显示本周/本月专注时间和最近产出。
5. 保持隐私边界：仍只记录前台应用进程名称和时长，不读取窗口标题、文件路径、键盘内容或浏览历史。

### B4. 稿件任务与项目关联

1. 稿件可以关联项目，稿件截止任务通过统一行动队列出现在主页。
2. 修回轮次截止日期与稿件任务之间建立明确引用或一致性校验，但不得静默覆盖用户手动设置的日期。
3. 从稿件工作区创建“建议催稿日期”后，主页应在对应日期显示。
4. 投稿重要更新可一键生成项目笔记，笔记中仅写安全的状态文本和时间，不写追踪链接或凭证。

### B5. 数据迁移与存储策略

1. 将 schema 从 7 升级到下一个明确版本，并为 projects、drafts 和新增关联字段编写迁移。
2. 迁移函数保持纯函数或可独立测试；迁移成功前不得写回原文件。
3. 如果继续使用单一 JSON，至少减少应用使用采样时整份数据的高频重写和全量 renderer 广播；将高频运行状态与低频持久化分开。
4. 如果改用 SQLite，必须提供从 schema 7 JSON 到新存储的可回滚迁移、原文件保留和完整测试。不要为了“看起来专业”仓促迁移数据库。

---

## 本轮明确不做

1. 手机 App、网页端或跨设备云同步。
2. 用户账号、在线协作、公开分享链接。
3. 登录 Editorial Manager、ScholarOne 或其他出版社后台抓取状态。
4. 邮箱账号直连、邮件自动读取或后台监控。
5. 完整 PDF 管理、全文翻译、AI 论文写作或系统综述平台。
6. 商业 JCR 数据接入。
7. 便携版安装包。

可以在最终报告中提出“出版社邮件/剪贴板导入”作为 v1.1 候选，但本轮不要半实现入口。

---

## 代码质量要求

1. 不要继续无限扩张单个 `src/main.js`、`src/renderer/app.js`、`src/renderer/workbench.js` 和 `styles.css`。
2. 按领域拆分主进程服务、数据模型、IPC、renderer 页面逻辑和样式，同时保持现有 CommonJS/无前端框架架构，除非能证明引入框架的迁移收益和风险。
3. IPC 通道使用最小权限和输入验证；所有 renderer 文本继续转义。
4. 新增错误信息必须短、可操作且不含敏感数据。
5. 不添加未使用依赖；如必须新增依赖，解释用途、许可证、体积和是否属于核心依赖。
6. 不创建临时 `.py` 文件，不修改原始用户数据，不打印任何 `.env` 或密钥内容。

---

## 最终验收

至少完成以下验证：

1. 所有单元测试通过。
2. 数据迁移测试覆盖 schema 7 正常数据、损坏数据、未来版本数据和迁移中途失败。
3. 备份、恢复、草稿、项目关联、统一行动队列和专注关联均有测试。
4. Electron 全页面冒烟测试通过，并逐张检查生成截图；结构检查不能冒充视觉 QA。
5. 实际验证中文输入法、快捷捕获、笔记草稿、周日程看板、3:4 桌面日程组件、项目切换和投稿任务跳转。
6. 运行安装版构建：`npm run dist`，只生成 x64 NSIS 安装包。
7. 检查安装包、blockmap 和 `latest.yml`，确认版本一致。
8. 更新 `README.md`、版本号、数据 schema 说明、隐私边界、迁移说明和已知限制。

如果环境中 `npm` 不在 PATH，先定位项目或工作区已有的 Node/npm/pnpm 运行时；不要因此跳过测试。

---

## 最终交付报告格式

完成后按以下结构汇报：

1. **已完成**：按阶段和功能列出实际实现内容。
2. **关键设计决定**：说明统一行动队列、项目关联、草稿和备份的实现方式。
3. **数据迁移**：旧版本如何迁移、原文件如何保护、失败如何回滚。
4. **隐私与安全**：说明哪些数据会联网、哪些只保存在本机、哪些字段被导出排除。
5. **验证结果**：逐条列出实际运行的命令、测试数、冒烟页面、截图检查和安装包结果。
6. **未完成或降级内容**：如实列出任何未实现、仅部分实现、无法验证或依赖环境的事项，不得用模糊措辞掩盖。
7. **修改文件**：列出主要修改文件及用途。
8. **建议下一步**：只列 3 项以内，并明确不属于本轮交付。

不要仅提交代码而不运行验证，也不要因为测试通过就声称完成了真实页面视觉检查。
